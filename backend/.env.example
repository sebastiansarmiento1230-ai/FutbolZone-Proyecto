# ================================================
# VARIABLES DE ENTORNO — FUTBOLZONE (SENA ADSO III)
# ================================================

# Base de Datos (Opcional MySQL - Fallback automático a SQLite)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=futbolzone

# Seguridad y JWT
SECRET_KEY=futbolzone_secret_key_super_segura_2026
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=120

# Frontend URL
FRONTEND_URL=http://localhost:5173

# ================================================
# CONFIGURACIÓN DE ENVÍO DE CORREO REAL (SMTP)
# ================================================
# Para usar Gmail:
# 1. Ve a tu cuenta de Google -> Seguridad -> Verificación en 2 pasos (activarla).
# 2. Busca "Contraseñas de aplicaciones" y crea una para "FutbolZone".
# 3. Pega tu correo en SMTP_USER y la clave de 16 caracteres en SMTP_PASSWORD.

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=nicolasfelipepazortiz@gmail.com
SMTP_PASSWORD=vjob ddfy ykkk yexh
SMTP_FROM=no-reply@futbolzone.com
SMTP_FROM_NAME=FutbolZone Oficial
SMTP_TLS=true
SMTP_SSL=false
